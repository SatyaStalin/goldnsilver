const express = require('express');
const router = express.Router();

// SIP routes placeholder
router.get('/', async (req, res) => {
  res.json({ message: 'SIP routes - coming soon' });
});

module.exports = router;
